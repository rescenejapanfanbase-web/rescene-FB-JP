
(()=>{const grid=document.getElementById('allNews'),buttons=[...document.querySelectorAll('.filter')];if(!grid||!window.RESCENE_NEWS)return;const draw=f=>{const rows=f==='all'?RESCENE_NEWS:RESCENE_NEWS.filter(n=>n.category===f);grid.innerHTML=rows.map(n=>`<article class="news-card"><div class="news-meta"><span class="news-date">${n.date}</span><span class="badge">${n.label}</span></div><div class="news-title"><a href="${n.link}">${n.title}</a></div><div class="news-text">${n.text}</div></article>`).join('')};buttons.forEach(b=>b.addEventListener('click',()=>{buttons.forEach(x=>x.classList.remove('active'));b.classList.add('active');draw(b.dataset.filter)}));draw('all')})();
